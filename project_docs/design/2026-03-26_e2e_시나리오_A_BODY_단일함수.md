# E2E 시나리오 A - BODY 변경 단일 함수 검증

## 목적

- 가장 일반적인 로직 수정 상황에서 영향도 분석, change log, 리뷰/생성 결과가 일관되게 나오는지 검증한다.

## 대상 기준

- SCM ID: `hdpdm01`
- Source Root: `D:\Project\Ados\PDS64_RD`
- linked docs 기준:
  - UDS: `D:\Project\devops\260105\backend\reports\uds_local\uds_spec_generated_expanded_20260325_041213.docx`
  - SUTS: `D:\Project\devops\260105\reports\suts\suts_impact_20260324_111833.xlsm`
  - STS: `D:\Project\devops\260105\reports\sts\sts_eval_20260319_123007.xlsx`

## 실제 대상 파일/함수

- 대상 파일: `D:\Project\Ados\PDS64_RD\Sources\APP\Ap_BuzzerCtrl_PDS.c`
- 대표 함수:
  - `g_Ap_BuzzerCtrl_Func`
  - `s_BuzzerCtrl_On`
  - `s_BuzzerStateCtrl`

## 변경 방식

- 함수 시그니처, 선언부, 헤더 파일은 건드리지 않는다.
- `s_BuzzerCtrl_On` 또는 `s_BuzzerStateCtrl` 내부 분기/상수/조건식만 최소 단위로 수정한다.
- 예시:
  - duty 비교 조건 변경
  - 내부 분기 순서 조정
  - 로컬 변수 계산식 수정

## 기대 결과

- changed function type: `BODY`
- changed_files에 `Sources/APP/Ap_BuzzerCtrl_PDS.c` 포함
- changed_functions에 `g_ap_buzzerctrl_func`, `s_buzzerctrl_on`, `s_buzzerstatectrl` 중 최소 1개 이상 포함
- audit 파일 생성
- change log 생성
- UDS/SUTS/SITS 중 정책상 대상 문서에 review 또는 생성 결과 기록
- 프론트엔드에서 BODY 변경 중심 가이드 노출

## 주요 확인 포인트

- BODY 변경이 `SIGNATURE`로 잘못 분류되지 않는가
- change log summary가 전부 0으로만 남지 않는가
- review-required가 단순 실패가 아니라 정책상 판정으로 보이는가
- 프론트엔드 안내 문구가 BODY 변경 해석과 일치하는가

## 실행 절차

1. `Ap_BuzzerCtrl_PDS.c` 기준 상태 백업
2. `s_BuzzerCtrl_On` 또는 `s_BuzzerStateCtrl` BODY만 로컬 수정
3. 로컬 영향도 분석 실행
4. 생성된 audit/change log 경로 수집
5. UDS/SUTS/SITS 결과 경로 또는 review 문서 경로 기록
6. 프론트엔드 `Local SCM` 영역에서 영향 요약 확인

## PASS 기준

- `BODY` 판정이 나온다
- change log가 생성된다
- changed_files/changed_functions가 기대 범위 내에 있다
- 결과를 운영자가 설명할 수 있다

## 금지 사항

- SVN commit 금지
- 원격 반영 금지
- 불필요한 다중 파일 수정 금지

