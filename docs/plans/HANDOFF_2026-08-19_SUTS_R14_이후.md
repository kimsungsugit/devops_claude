# 인계 — SUTS 정본 대조 R14 이후 (2026-08-19, **R22 까지** 갱신)

> 다음 세션이 **이것부터** 읽는다. 계획서 본체는
> `C:\Users\kss11\.claude\plans\partitioned-soaring-fiddle.md`,
> 누적 교훈은 메모리 `project_suts_array_expansion`.
>
> R19·R20·R17·**R22** 는 코드 로직을 한 줄도 안 바꿨다(축 전부 기각/종결).
> **R21 이 관찰 첨자 축을 열어 입력 +27 · 기대 +53 을 얻었다** — 아래 표는 R21 반영본이다.
> **R22 는 그 '입도차' 라는 숫자 자체가 두 축이었음을 찾아냈다** (아래 §R22).

## 지금 어디까지 왔나

| | 정본 | 일치 | 미달 | 과다 |
|---|---|---|---|---|
| 입력 Inpt | 6,014 | 5,459 (**90.8%**) | 555 | 1,451 |
| 기대 ExpR | 5,389 | 4,980 (**92.4%**) | 409 | 683 |

## ⚠ 미달 분해가 틀려 있었다 — 한 겹 더 쪼개진다 (R19 부산물)

R14 인계본은 미달을 `입도차 + 이름부재` 2분류로 적었다. **이름부재의 절반 이상이
재현 대상이 아니다** — 정본이 같은 이름을 **입력·기대 양쪽에** 적은 것이고, 우리는
한쪽에만 냈다. VectorCAST 가 입력값을 기대 열에도 기록하는 실행 관측 산물이다.

    입력 555 = 입도차 319 · 방향오배치  37 · **진짜 이름부재 199**
    기대 409 = 입도차 181 · 방향오배치 129 · **진짜 이름부재  99**
                              ↑ 재현 대상 아님(아래 R19)

**축을 고를 땐 이 3분류를 쓸 것.** 2분류로 보면 "기대 이름부재 228" 이 최대 축처럼
보이는데 실제로 손댈 수 있는 건 99 다.

### ⚠ R22 가 한 겹 더 쪼갰다 — **입도차 500 은 두 축이었다**

`입도차 = 입력 319 + 기대 181 = 500` 을 (unit, 이름) 쌍의 **칸수 비교**로 다시 쪼개면:

    A 표기차   205칸 (41.0%)   정본칸 == 우리칸. 첨자 표기만 다르다(`Addr` vs `Addr[0]`)
    B 입도차   288칸 (57.6%)   정본칸 >  우리칸. 진짜로 더 펼쳐야 한다
    C 역전       7칸 ( 1.4%)   정본칸 <  우리칸. 우리가 과잉(`g_DoorState_his`)

**A 는 폭을 안 늘린다** — 예산도 안 먹는다. 그래서 "입도차 500" 을 통짜로 보면
"절반은 캡만 늘리면 되나" 로 오독하기 쉬운데, 실제 확장 대상은 288 이다.
R9 가 SITS 에서 겪은 것과 같은 형태다: **한 숫자에 두 축이 섞여 있었다.**

## 이번 세션 (R19·R20·R17) — 전부 기각/종결

| 축 | 규모 | 결과 |
|---|---|---|
| **R19 방향 판정** | 166칸 | ❌ **기각** — 대상 89 이름족 중 `[IN]` 태그가 **79 (89%)** |
| **R20 포인터 버퍼 입도** | 111칸 | ❌ **기각** — 적중 3칸 · 39.6% 가 정본보다 **과잉** |
| **R17 2D flatten** | 55칸 | ⛔ **종결 확정** — `tl_pdu` 는 포인터, 첫 차원이 선언에 없다 |

---

## ✅ R21 — 관찰 첨자 축 (채택)

**"포인터라 정적 유도 불가" 로 종결돼 있던 축인데, 파서가 답을 이미 갖고 있었다.**
본문에서 관찰한 첨자를 `(idx: …)` 로 싣고 있었고 소비처가 `_PARAM_ANNOT_KEYS` 로
**벗겨내기만** 했다(R10 이 관찰 첨자를 기각한 뒤로 쓰지 않았다).

    입력 5,432 (90.3%) → **5,459 (90.8%)**   과다 ±0
    기대 4,927 (91.4%) → **4,980 (92.4%)**   과다 +1 · **사라진 맞춤 0**

### 방향 태그가 곧 열이다 — 이게 이 라운드의 전부

    [IN]  → **입력** 열   적중 28 · 과다  1
    [OUT] → **기대** 열   적중 53 · 과다  0
    둘 다                 적중 81 · 과다  1 · 사라진 맞춤 0   ← 채택
    [OUT] 를 양쪽에       적중 63 · 과다 43 · 사라진 맞춤 1   ← 대조군

정본은 `[OUT]` 버퍼를 **입력 열엔 base 한 칸**으로 적는다(`sf_ReadAppVersion::
versionOut` 입력 1칸 · 기대 4칸). `[INOUT]` 은 적중이 **한 칸도 안 늘어** 제외.

⚠ **R10 을 뒤집은 게 아니다.** 10차가 기각한 건 **선언 크기가 있는 배열**에서다
(일치 146 → 10). `(size:)` 를 어느 경로로든 얻으면 이 규칙은 닿지 않는다.

⚠ 첨자가 **하나라도 변수식이면 슬롯을 통째로 버린다**. 리터럴만 골라 쓰면 폭이
임의로 좁아져 "이 배열은 이 칸만 시험한다" 는 없는 사실을 적는다.

### 뮤테이션 첫 판 4건 생존 — 전부 내 테스트 공백이었다 (교훈)

| # | 생존 이유 |
|---|---|
| M6 | `7*8` 이 리터럴로 통과해 `int()` 가 **터진다**. 앵커는 있었는데 안 쟀다 — 가정이 아니다, 정본이 `tl_pdu[7*8]` 로 41칸 적는다 |
| M12 | `versionOut` 을 기대 열에만 둬서 **입력 열 축이 재어지지 않았다**(잘못된 구현도 통과) |
| M15 | 헬퍼 단독 테스트는 **호출부가 넘기는 태그 집합**을 못 본다 |
| M9 | **등가였다** — dims 없으면 `at_node` 는 이미 -1. 죽은 방어라 제거 |

보강 후 **14/14 죽음 · 생존 0**. 검증은 총계가 아니라 **일치 집합 뺄셈**으로 했다.

### SITS 영향 없음

`_expand_array_entries` 는 SUTS·SITS 공용이나 `observed_idx` 는 기본값 None 이고
SITS(`generators/sits.py:689`)는 안 넘긴다. 새 반환 키 `observed` 도 SITS 는 안 읽는다.
**SITS 에 같은 개선을 적용할 여지가 있다** — 별도 시리즈라 이번 범위 밖.

---

## ⚠ 다시 파지 말 것 (근거와 숫자)

### R19 방향 판정 축 — **기각**

`s_sha256_final(SHA256_CTX *ctx, __far U8 *hash)` 에서 정본은 `hash` 를 기대 열
32칸으로 적는데 우리는 입력 열에만 냈다. "out 파라미터를 기대 열에 안 넣는다"는
결함처럼 보였다. 전수로 재니 **정반대**였다:

    대상 89 이름족의 파서 태그:  [IN] 79 (89%) · 파서에 없음 6 · [INOUT] 2 · [OUT] 1(+1)

    규칙 후보                     적중   과다   정밀도
    outputs 의 [OUT]                2     13    13.3%
    outputs 의 [OUT]+[INOUT]        4     84     4.5%
    전체 슬롯 [OUT]+[INOUT]         4    423     0.9%
    전체 [INDIRECT*]                0  2,446     0.0%

즉 정본이 기대 열에 넣은 건 파서가 `[IN]` 으로 **정확히** 판정한 것들이다. 정적
유도로 재현할 축이 아니다(VectorCAST 가 입력값을 양쪽에 기록한 것).

곁가지 확인: SwUDS 우선 정책이 소스의 `[OUT]` 을 덮어써 버리는 건 **사실**이다
(89칸). 그런데 그중 정본이 원하는 건 4칸이고, 나머지는 이미 종결 선언된
`SafeWriteQueue 중간마디` 축이다. **뚫을 값이 없다.**

출처 판정(전수): 정본↔SwUDS 불일치 119 · 소스추론 유래 47 ·
**"우리가 문서를 안 따랐다" 0칸**. SwUDS 는 우리 편이다.

### R20 포인터 버퍼 입도 정합 — **기각**

사용자 결정(이름은 우리 것 유지, 입도만 정본과 맞춤)에 따라 쟀다. 새로 나는 111칸:

    63 (56.8%)  VECTORCAST 자리 — 이름 결정상 지표엔 안 잡히나 입도는 정본과 동일
    44 (39.6%)  정본은 같은 이름을 **다른 입도**로 적음 → **과잉**
     3 ( 2.7%)  적중 (`FTMRZ_Prog_Phrase::PS.Add.ByteArray[1..3]`)
     1 ( 0.9%)  정본이 그 이름을 아예 안 적음

정본은 `RespMsg.data` 를 **1칸**으로 적는데(입력 열, 포인터 값) 새 규칙은 3~4칸으로
펼친다. 반대로 `ctx.state` 는 정본도 8칸이다. **둘을 구분할 신호가 없다** — 양쪽 다
"포인터 파라미터의 멤버". R16 기각(폭 좁히기)과 대칭인 형태다.

⚠ 열 상한 96칸이 있어 과잉 확장은 **뒤 변수를 밀어낸다**(캡 규약).
⚠ 사용자에게 제시한 선택지에 "시험 칸수는 정본과 동일해짐" 이라고 적었는데
   **56.8% 만 그렇다**. 선택지를 만들 때 그 분해를 먼저 쟀어야 했다.

### R17 `tl_pdu[N*8]` — **종결 확정** (인계본의 "다음 단계" 를 실행한 결과)

R14 인계본은 "선언을 소스에서 직접 확인하는 게 다음 단계" 로 멈췄다. 찾았다:

```c
typedef l_u8 lin_tl_pdu_data[8];   // lin.h:179   ← 둘째 차원 8 은 여기서
lin_tl_pdu_data  *tl_pdu;          // lin.h:559   ← **포인터**. 첫 차원은 런타임 할당
#define MAX_QUEUE_SIZE 7           // lin_cfg.h   ← 정본의 `7`
```

정본 표기 55칸 = `[7*8]` 33 · `[1*8]` 7 · `[4*8]` 1 · base 3 · `[i][j]` 11.
`[N*8]` 41칸의 N 은 **실행 시 할당된 행 수**라 정적 유도 불가다. 게다가 `lin.h` 는
`verify=X` 파일이라 파서가 안 읽는다 — **11차의 `g_LinIntCtrl.dataBuffer` 와 같은 벽**
(인계본의 예견이 맞았다). verify 필터를 풀어도 `[8]` 만 얻고 `7` 은 못 얻는다.

`[i][j]` 리터럴 11칸만 소스 본문에서 유도 가능하나, 그러려면 아래 파서 비대칭을
고쳐야 하고 그건 44칸 과잉을 동반한다(R20). **값이 안 맞는다.**

---

## 알려진 파서 결함 — **고치면 나빠진다** (지우지 말 것)

`report_gen/function_analyzer.py:_scan_name_usage` 에 비대칭이 있다:

```python
# 멤버 경로는 체인을 끝까지 문다
for m in re.finditer(rf"\b{name}\b({_MEMBER_CHAIN_RE})", line):   # RespMsg->data     ✓
    u["members"].add(...)
# 그런데 첨자는 name 바로 뒤 `[` 만 잡는다
for m in re.finditer(rf"\b{name}\b\s*\[\s*([^\]]+)\s*\]", line):  # RespMsg->data[0]  ✗
    u["indexes"].add(...)
```

`hash[i]`·`Values[1]` 처럼 **이름 직후** 첨자는 잡히고(산출물에 `(idx:)` 140 슬롯),
`RespMsg->data[0]` 처럼 **멤버 경로 뒤** 첨자는 통째로 누락된다.

**누가 봐도 버그다. 그런데 고치면 정본과 멀어진다** — 얻는 슬롯 76개(리터럴 39 ·
변수식 37) 중 정본 적중은 3칸이고 44칸이 과잉이다(R20). 게다가 소비처
(`generators/suts.py`)는 `(idx:)` 를 확장에 **안 쓴다**(R10 이 관찰 첨자를 기각한 결과,
`_PARAM_ANNOT_KEYS` 로 벗겨내기만 한다). 즉 파서만 고치면 **산출물이 안 변한다**
= 관측 불가한 변경이다.

→ **손대지 않는다.** 다른 축에서 멤버 경로 첨자가 꼭 필요해지면 그때 이 항목부터 읽을 것.

---

## ⚠ 이번 세션의 측정 오류 3종 — 전부 "100% 한 분류로 몰림" 이 신호였다

같은 함정을 다음 세션이 또 밟지 않도록 남긴다. **셋 다 결과가 그럴듯해서** 하마터면
그대로 결론을 냈다:

| # | 오류 | 증상 | 교정 |
|---|---|---|---|
| 1 | `uds_io` 를 top-level 조회 | 판정 166칸이 **전부** "SwUDS 에 없음" | `uds_io["by_name"]` (1,017 unit) |
| 2 | `function_details` 를 함수명으로 조회 | "파서 보유 **0.0%**" | 키는 `SwUFn_0101`, 함수명은 값의 `name` (실제 **100%**) |
| 3 | `[OUT] __far U8 * hash` 에서 타입 미제거 | 태그 매칭 **적중 0** | `generators.suts._extract_var_names` 재사용 |

**규칙: 전수 판정이 한 분류로 100% 몰리면 데이터가 아니라 내 조회 경로를 먼저 의심한다.**
그리고 파싱은 **새로 만들지 말고** 소비처 함수를 쓴다(3번은 이 시리즈가 4번째로 밟은
"변수명에 타입 접합" 함정이다).

---

## R22 — 미탐색 잔여 전수 귀속 (**코드 로직 변경 없음**)

인계본의 "다음 후보" 표를 실행한 결과다. **후보가 전부 기존 벽에 걸린다**는 것을
수치로 확정했고, 그 과정에서 위 A/B/C 분해를 찾았다.

### A `[0]` 표기 축 — ❌ **기각** (205칸 중 130칸이 첨자 `0`)

분모를 좁히지 않고 "우리가 base 한 칸으로 낸 (unit, 이름) **전부**" 로 쟀다:

| 열 | base 로 낸 쌍 | 정본**도** base | 정본 = `[0]` 한 칸 |
|---|---|---|---|
| 입력 | 3,489 | **2,600 (74.5%)** | 62 (1.8%) |
| 기대 | 2,635 | **2,295 (87.1%)** | 64 (2.4%) |

`[0]` 을 붙이면 **4,895칸을 잃고** 126칸을 얻는다. 좁히는 규칙을 6개 시험했는데
손실 0 이 하나도 없다:

    R0 전부                   적중 156 · 손실 4,895
    R1 (idx:) == {0} 뿐        적중   0 · 손실     0   ← 얻을 게 없다
    R2 (idx:) 에 0 포함        적중  10 · 손실     1
    R3 (idx:) 有 · (size:) 無  적중  29 · 손실     8
    R4 슬롯이 배열/포인터꼴     적중 139 · 손실 4,205
    R5 (size:) 보유            적중   1 · 과다    19

**정본의 지배 표기는 base 다.** 이 근거는 `_observed_idx_map` 의 `len(idxs) <= 1`
분기 주석에도 박아 뒀다 — 첨자 1개짜리를 왜 안 쓰는지가 거기 있다.

### B 입도차 288칸 — 전량 귀속했다

| 귀속 | 칸 | 상태 |
|---|---|---|
| LIN / `tl_pdu` | 162 | ⛔ 종결(실행 실측 · 포인터 첫 차원 미상) |
| 구조체 멤버(`ctx.state` 등) | 72 | ❌ R20 기각 — **아래 주의** |
| `[INOUT]` | 1 | ❌ R21 기각 |
| `pe_StateHistory` | 32 | ⛔ **신규 종결** — `[INOUT] enum … * pe_StateHistory (idx: 0, u16t_DstIdxU, u16t_SrcIdxU)`. 포인터 + 변수 첨자라 폭이 없다 |
| `RVL` | 18 | ⛔ **신규 종결** — 파서 슬롯 **0개** · `globals_info` 에도 없다. SwUDS 문서에 이름만 있고 소스엔 그 이름이 없다 |
| `Values` | 9 | ❌ **기각**(아래) |
| `u8char` · `Addr` 등 롱테일 | 118 | ⛔ 포인터인데 `(idx:)` 조차 없다 |

### ⚠ `struct_member_arrays` 에 답이 있는 것처럼 보이지만 아니다

`SHA256_CTX -> {'state': '[8]', 'buffer': '[64]'}` 가 **파서 산출물에 실제로 들어 있다**.
그래서 "조회 경로만 이으면 `ctx.state` 24칸이 열린다" 로 보이는데, `_struct_member_dims`
가 포인터 파라미터를 **일부러** 제외하고 있다(R20: 일치 +34 에 과다 **+176**).
정본은 `ctx.buffer` 를 선언 64칸이 아니라 **9칸**(`[0]`, `[56..63]`)만 적는다 — 실행
관측이라 선언 크기와 무관하다. **이 8개짜리 맵을 보고 다시 열지 말 것.**

### `Values` 9칸 — `[OUT]→입력` 은 여전히 손실 1

`[OUT]` + 리터럴 `(idx:)` 슬롯은 저장소 전체에 **7개**뿐이고 R21 이 이미 소진했다.
남은 `Values` 를 위해 "이름이 `inputs` 키에도 있을 때만" 이라는 새 조건을 시험했는데
**`Values` 와 `versionOut` 이 둘 다 걸려 못 가른다**:

    (A) 조건 없이           적중 10 · 손실 1 · 과다 43
    (B) inputs 동시등장     적중  9 · 손실 1 · 과다  4   ← 손실 0 아님 → 기각

### R22 의 자기 정정

`(idx:)` 슬롯 401개를 관문별로 분류하면서 `[INOUT]` 이 **124칸**을 놓치는 것처럼
쟀는데, 그건 실제 생성물과 대조하지 않은 **상한**이었다. 라이브로 다시 재니
`[INOUT]→양쪽` 은 적중 81 그대로(증가 0) · 과다만 18→34. **R21 판정이 정확했다.**
*"새 측정이 기존과 다르면 새 측정부터 의심하라"* 가 또 맞았다.

---

## 다음 후보 — **R22 가 전부 닫았다**

미탐색 입도차 항목은 R22 에서 전수 귀속됐다(위 §R22 B 표). 남은 것은 전부
"크기를 지어내야만 닿는" 축이라 이 시리즈의 불변 규약에 걸린다.

**다른 방향으로 가야 한다면** 아직 안 건드린 표면은 이것들이다:

| 후보 | 성격 | 비고 |
|---|---|---|
| 과다 1,451 / 683 | **미측정** — 지금껏 미달만 팠다 | R18(verify=X 152칸)이 그 일부. 과다는 "정본에 없는 걸 우리가 적음" 이라 **정직성 축**이지 정합 축이 아니다 |
| SITS 에 R21 이식 | 공용 헬퍼인데 `observed_idx` 를 안 넘긴다 | `generators/sits.py:689`. **별도 정본 대조 시리즈** — 그쪽 지형부터 재고 들어갈 것 |
| 이름부재 199 / 99 | 3분류 중 유일하게 미착수 | 입도가 아니라 **이름 자체**가 없다. R14 이후 아무도 안 쟀다 |

⚠ **입도차 축으로 다시 들어오지 말 것.** 500칸 전량이 위 표에 귀속돼 있고,
그중 손댈 수 있는 건 0 이다.

## 종결 선언 (더 파지 않는다)

| 축 | 규모 | 이유 |
|---|---|---|
| 포인터 파라미터 원소 수 | 371 → **~290칸** | 정적 유도 불가 — 원소 수는 호출자만 안다.
  ⚠ **R21 이 일부를 뒤집었다**: 원소 **수**는 못 얻어도 본문이 만진 **첨자**는 파서가 이미 안다.
  남은 건 본문에 리터럴 첨자가 없는 것들(루프·변수 첨자) |
| LIN `lin_pFrameBuf` 등 | 60칸 | 정본 수치가 VectorCAST **실행 실측** |
| `tl_pdu[N*8]` | 41칸 | **신규 종결** — 포인터라 첫 차원이 선언에 없다(위 R17) |
| `st_EepromSafeQueue` 중간마디 | 10칸 | 이름 부재 — 찾아도 1일치/15과다 |
| 11차 잔여 16칸 | 16칸 | 타입 선언이 `verify=X` 파일(`lin.h`) |
| `VECTORCAST_STR1` | 112칸 | **정책상 재현 안 함** — 사용자 결정(2026-08-19): 도구 자동 버퍼명 대신 설계 이름 유지 |
| 방향 오배치 | 166칸 | **신규 종결** — 정본이 입력값을 양쪽에 적은 실행 관측 산물(위 R19) |
| **`[0]` 표기차** | **205칸** | **R22 신규 종결** — 정본의 지배 표기가 base 다. 붙이면 4,895칸 손실 |
| **`pe_StateHistory`** | **32칸** | **R22 신규 종결** — 포인터 + 변수 첨자(`idx: 0, u16t_DstIdxU, …`) |
| **`RVL`** | **18칸** | **R22 신규 종결** — 파서 슬롯 0 · 소스에 그 이름이 없다(문서 유래) |
| **구조체 멤버 재개봉** | 72칸 | **R22 재확인** — `struct_member_arrays` 에 `SHA256_CTX` 가 보여도 열지 말 것(R20: 과다 +176) |

**크기를 지어내지 않는다** — 이 시리즈의 불변 규약이다.

## 미결 (사용자 결정 대기)

**R18 verify=X unit 89개 / 과다 152칸** — 2026-08-19 "지금은 보류" 로 결정됨.
정본도 X unit 을 25개 담으므로 "전부 제외" 는 틀렸고, 과다 830 중 X 유래는 18% 뿐이라
89개를 다 빼도 5분의 1도 안 준다. 표는 `r18_verify_scope.py` 로 재생성 가능.

## 재사용 자산 (재작성 금지)

이번 세션 스크래치패드
`…/Temp/claude/D--Project-devops-Release-claude/8be4339d-…/scratchpad/`:

| 파일 | 용도 |
|---|---|
| `r15_landscape.py` | **지형 재측정** — 3분류 분해 전 판. 캐시 hit 면 ~30초 |
| `r19_direction.py` | 방향 오배치 3분류 분해 (미달을 입도/방향/이름부재로) |
| `r19_origin.py` | 방향의 출처 판정(SwUDS vs 소스 추론) |
| `r19_rule3.py` | 태그 기반 규칙 정밀도 — **`_extract_var_names` 재사용 판** |
| `r20_phase0.py` · `r20_scope.py` · `r20_decomp.py` | 첨자 유도율 · 영향 범위 · 과다 분해 |
| `r17_probe.py` · `r17_decl.py` | tl_pdu 표기·선언 직독 |

이전 세션(`d0cfcfa8-…`) 자산도 살아 있다 — `live_cmp.py`(라이브↔정본 4숫자) ·
`mut_r14.py`(뮤테이션 하네스) · `r18_verify_scope.py`.

정본 실물은 로컬에 있다 — `%TEMP%/_ref_suts.xlsm` · `_ref_swuds.docx` ·
`_array_dump.json`. **Cloudium 왕복 불필요.**

⚠ `_array_dump.json` 의 `gen_*` 는 **R14 이전** 값이다. 라이브로 다시 뽑을 것
(`r15_landscape.py` 방식). `ref_in`/`ref_out` 은 정본이라 그대로 써도 된다.

⚠ `source_root` 는 **콤마로 이어진 2개 트리**다
(`C:\Project\Ados\NE1AW_PORTING,C:\Project\Ados\PDS128_FBL`). 단일 경로로 읽으면
`exists()` 가 False 라 "소스 없음" 으로 오독한다.

## 작업 환경 주의

- **동시 세션이 같은 트리에서 돈다.** 2026-08-19 에 린트 backlog 정리(ruff 907→141,
  eslint 91→53)가 `generators/suts.py` 를 포함한 208 파일을 staged 로 올렸다.
  순수 린트라 로직 무관이지만 **index 에 남의 파일이 있는 채로 커밋하면 내 메시지로
  들어간다**. 절차는 메모리 `reference_git_commit_index_hazard` — `git add` →
  `git diff --cached --name-status` 로 **D 0건과 목록을 눈으로** → pathspec 없이 커밋 →
  `git show --name-only` 재검증.
- heredoc 으로 패치를 넣지 말 것(백슬래시 소실). `Write` 도구를 쓸 것.
- **에이전트 14종이 전부 `model: opus`** — `deep-reviewer` vs `reviewer` 는 이제
  모델이 아니라 **의무**로 갈린다.
