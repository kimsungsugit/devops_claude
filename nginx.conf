server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    client_max_body_size 50M;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;
    gzip_min_length 1000;

    # API proxy (with SSE / streaming support)
    location /api/ {
        proxy_pass http://aria-backend:9000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE / long polling requires disabling buffering + long timeouts
        # 600s→1800s: 대형 SwUDS(예 53MB) 첫 impact 분석은 in-process 파싱이 ~400s+ 걸려
        # 600s에서 504로 잘릴 수 있다(2회차부터는 _UDS_NAME_ASIL_CACHE로 즉시). 30분 헤드룸.
        proxy_buffering off;
        proxy_cache off;
        proxy_set_header Connection "";
        proxy_read_timeout 1800s;
        proxy_connect_timeout 60s;
        proxy_send_timeout 1800s;
        chunked_transfer_encoding on;
    }

    # Static assets with cache headers
    location ~* \.(js|css|svg|png|jpg|jpeg|gif|ico|woff2?)$ {
        try_files $uri =404;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
}
