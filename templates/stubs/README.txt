Stub templates live here.

These files are used by workflow/stubs.py to generate test stubs.
If a template file is missing, stubs.py will export the inline fallback
content into this directory automatically on the next run.
